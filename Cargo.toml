[package]
name = "test_build"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
bumpalo = { version = "3.16.0", features = ["boxed"] }
either = "1.15.0"
lazy_format = "2.0.3"
libR-sys = "0.6.0"
prettydiff = "0.8.0"

[dev-dependencies]
insta = { version = "1.36.1", features = ["yaml"] }

